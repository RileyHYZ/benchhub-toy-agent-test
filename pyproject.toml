[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "toy-agent"
version = "0.1.0"
description = "A toy custom agent package for BenchHub testing"
requires-python = ">=3.10"
dependencies = []
